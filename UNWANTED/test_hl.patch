--- frontend/src/components/PDFViewerModal.jsx
+++ frontend/src/components/PDFViewerModal.jsx
@@ -462,6 +462,8 @@
       const spans = Array.from(textLayer.querySelectorAll('span'));
       if (spans.length === 0) return;
 
+      console.log("[PDFViewer] applyHighlight called! Spans found:", spans.length, "textLayerHandled:", textLayerHandled);
+
       textLayerHandled = true;
       if (observer) observer.disconnect();
 
@@ -476,6 +478,8 @@
       const medianFontSizePx = fontSizes.length
         ? fontSizes[Math.floor(fontSizes.length / 2)]
         : 0;
+        
+      console.log("[PDFViewer] pageCompact length:", pageCompact?.length, " median font size:", medianFontSizePx);
 
       // ── Build word token map ──
       const { wordMap, pageCompact, charToWordIdx } =
