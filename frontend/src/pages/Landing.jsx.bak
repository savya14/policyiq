import { useNavigate } from 'react-router-dom';
import logo from '../assets/iocl_logo.jpg';

export default function Landing() {
  const navigate = useNavigate();

  return (
    <div className="min-h-screen bg-white font-sans page-enter">
      {/* Nav */}
      <nav className="flex items-center justify-between px-8 py-5 border-b border-slate-100">
        <div className="logo-container-custom flex items-center gap-2 hover:opacity-90 transition-opacity">
          <img src={logo} alt="IOCL" className="logo-main" />
          <div className="flex flex-col">
            <span className="text-[24px] font-bold text-[#1e2d78] leading-tight tracking-tight font-sans">IndianOil</span>
            <span className="text-[13px] font-medium text-slate-500 tracking-[0.15em] uppercase leading-none mt-0.5 font-sans">Policy Copilot</span>
          </div>
        </div>
        <div className="flex items-center gap-3">
          <button
            onClick={() => navigate('/admin')}
            className="text-sm text-slate-500 hover:text-navy transition-colors relative after:absolute after:bottom-[-2px] after:left-0 after:w-0 after:h-[2px] after:bg-blue-500 after:transition-all after:duration-200 hover:after:w-full"
          >
            Admin
          </button>
          <button
            onClick={() => navigate('/chat')}
            className="text-sm bg-navy text-white px-4 py-2 rounded-full hover:bg-navy-light transition-all duration-200 hover:scale-105 hover:shadow-lg"
          >
            Open Chat →
          </button>
        </div>
      </nav>

      {/* Hero */}
      <section className="max-w-5xl mx-auto px-8 pt-24 pb-20 text-center">
        <span className="inline-block text-xs font-semibold tracking-widest text-orange uppercase mb-6 border border-orange/30 px-3 py-1 rounded-full">
          OISD · PESO · MoPNG Compliance Assistant
        </span>

        <h1 className="text-5xl md:text-6xl font-black text-navy leading-tight mb-4">
          Compliance answers,
          <br />
          <span
            style={{
              background: 'linear-gradient(135deg, #f57c00 0%, #e64a19 100%)',
              WebkitBackgroundClip: 'text',
              WebkitTextFillColor: 'transparent',
              backgroundClip: 'text',
            }}
          >
            in seconds.
          </span>
        </h1>

        <p className="text-lg text-slate-500 max-w-xl mx-auto mb-10 leading-relaxed">
          Ask about OISD standards, PESO guidelines, and MoPNG circulars in plain English.
          Get cited, grounded answers — no PDF hunting.
        </p>

        <div className="flex items-center justify-center gap-3">
          <button
            onClick={() => navigate('/chat')}
            className="bg-navy text-white px-7 py-3 rounded-full font-semibold text-sm hover:bg-navy-light transition-all shadow-lg shadow-navy/20"
          >
            Ask a question →
          </button>
          <button
            onClick={() => navigate('/admin')}
            className="border border-slate-200 text-slate-600 px-7 py-3 rounded-full font-semibold text-sm hover:border-slate-300 hover:text-slate-800 transition-all"
          >
            Admin panel
          </button>
        </div>
      </section>

      {/* Stats */}
      <section className="border-t border-slate-100 border-b">
        <div className="max-w-3xl mx-auto px-8 py-12 grid grid-cols-3 gap-8 text-center">
          {[
            { value: '≤ 10s', label: 'Query response time' },
            { value: '100%', label: 'Source-cited answers' },
            { value: '0', label: 'Bytes leave network' },
          ].map((stat) => (
            <div key={stat.label}>
              <div className="text-3xl font-black text-navy mb-1">{stat.value}</div>
              <div className="text-xs text-slate-400 uppercase tracking-wider">{stat.label}</div>
            </div>
          ))}
        </div>
      </section>

      {/* How it works — 3 steps, honest not decorative */}
      <section className="max-w-4xl mx-auto px-8 py-20">
        <p className="text-xs font-semibold tracking-widest text-orange uppercase text-center mb-10">
          HOW IT WORKS
        </p>
        <div className="grid md:grid-cols-3 gap-6">
          {[
            {
              step: '01',
              title: 'Ask in plain English',
              body: 'Type your compliance question — minimum distances, inspection intervals, approval limits.',
            },
            {
              step: '02',
              title: 'Pipeline retrieves context',
              body: 'FAISS finds the top 5 relevant chunks from the indexed OISD, PESO, and MoPNG corpus.',
            },
            {
              step: '03',
              title: 'Grounded answer, cited',
              body: 'LLaMA 3 generates an answer strictly from retrieved context. Source document and page always included.',
            },
          ].map((item) => (
            <div key={item.step} className="bg-slate-50 rounded-2xl p-6 border border-slate-100">
              <span className="text-4xl font-black text-navy/20 block mb-3">{item.step}</span>
              <h3 className="font-bold text-navy text-base mb-2">{item.title}</h3>
              <p className="text-sm text-slate-500 leading-relaxed">{item.body}</p>
            </div>
          ))}
        </div>
      </section>

      {/* CTA banner — navy gradient matching reference */}
      <section className="mx-8 mb-16 rounded-2xl overflow-hidden"
        style={{ background: 'linear-gradient(135deg, #1a237e 0%, #311b92 100%)' }}
      >
        <div className="px-10 py-12 flex flex-col md:flex-row items-center justify-between gap-6">
          <div>
            <h2 className="text-2xl font-black text-white mb-2">
              Built for IOCL. Grounded in OISD, PESO & MoPNG regulations.
            </h2>
            <p className="text-slate-300 text-sm">
              Answers never come from model training — only from the indexed corpus.
            </p>
          </div>
          <button
            onClick={() => navigate('/chat')}
            className="flex-shrink-0 bg-orange text-white font-semibold px-8 py-3 rounded-full text-sm hover:bg-orange-dark transition-colors shadow-lg"
          >
            Open Chat →
          </button>
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t border-slate-100 px-8 py-6 flex items-center justify-between">
        <div className="flex items-center gap-2">
          <div className="w-6 h-6 rounded bg-navy flex items-center justify-center">
            <span className="text-orange font-black text-xs">P</span>
          </div>
          <span className="text-xs text-slate-400">PolicyIQ · Indian Oil Corporation Limited</span>
        </div>
        <span className="text-xs text-slate-300">OISD · PESO · MoPNG Corpus</span>
      </footer>
    </div>
  );
}